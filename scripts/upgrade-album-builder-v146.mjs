import { readFileSync, writeFileSync } from "node:fs";

console.log("Prompt Muse v14.6 AlbumBuilder package is already built. To upgrade an existing repo, copy this package over the repo root, run npm test, commit, and push.");
console.log("Commands:");
console.log("npm test");
console.log("git add .");
console.log("git commit -m \"Upgrade Prompt Muse to v14.6 AlbumBuilder\"");
console.log("git push origin main");
