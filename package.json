{
  "name": "prompt-muse-suno-v14-4-versionlabel",
  "version": "14.5.1",
  "private": true,
  "description": "GitHub-to-Vercel iPhone Suno prompt studio with lyrics-first generation, visible latest-version labels, and Vercel protected-preview manifest auth handling.",
  "scripts": {
    "check": "node --check api/polish.js && node --check api/health.js && node --check netlify/functions/polish.js && node scripts/verify.mjs",
    "test": "npm run check",
    "verify": "node scripts/verify.mjs",
    "brief": "node scripts/print-llm-brief.mjs"
  }
}
